# Daily Agent Research Roundup - 2026-08-01

## Verdict

The strongest new signal is that runtime state, evaluation, routing evidence, and spend are converging into explicit operator-owned control surfaces.

There is no Saturday arXiv announcement batch. Friday's strongest papers were already included in the July 31 synthesis, so this update promotes fresh release evidence instead of repeating them.

## Top findings

### 1. Microsoft Agent Framework 1.13.0 makes replayable state concrete

Reusable session stores, complete Responses-session persistence, replayable checkpoints, approval fixes, bounded MCP skill discovery, and cache-write telemetry now ship together.

Action now: pin the release and run crash, resume, compaction, approval replay, duplicate-call, and migration fixtures.

Implementability score: **0.90**

Source: https://github.com/microsoft/agent-framework/releases/tag/python-1.13.0

### 2. Google unifies development and production evaluation

Gemini Enterprise Agent Platform evaluations are GA with versioned metrics, offline experiments, production monitors, drift alerts, trace drill-down, user simulation, and environment fault simulation.

Action now: run one metric definition across CI and sampled production traces, while keeping deterministic outcome checks load-bearing.

Implementability score: **0.82**

Source: https://developers.googleblog.com/agent-and-model-evaluations-in-gemini-enterprise-agent-platform-are-now-ga/

### 3. Vercel pairs routing receipts with enforceable budgets

AI Gateway logs now show cost, tokens, latency, provider, region, retention controls, and the full fallback path. Team, project, and key budgets can reject further requests when any applicable limit is exhausted.

Action now: export receipts, enforce nested budgets, and test retry, cache, fallback, and BYOK accounting.

Implementability score: **0.92**

Sources:
- https://vercel.com/changelog/ai-gateway-logs
- https://vercel.com/changelog/ai-gateway-spend-budgets-and-alerts

### 4. GitHub adds team-scoped model policy

Enterprise administrators can establish a baseline and grant Optional models to teams. The control is useful, but least-restrictive union semantics mean one permissive team can widen access everywhere under that enterprise license.

Action now: attach team grants to data class, budget, training, expiry, and effective-permission audits before enabling the preview.

Implementability score: **0.68**

Source: https://github.blog/changelog/2026-07-31-enterprise-teams-model-policy-targeting-in-public-preview

## What is implementable now

1. Version session stores, checkpoints, approvals, and provider adapters together.
2. Use the same metric registry in CI and production monitoring.
3. Export router receipts and enforce team, project, and key budgets.
4. Diff effective model access per user before policy migration.

## Highest and lowest implementability

Highest: **0.92**, Vercel AI Gateway logs plus nested spend budgets.

Lowest: **0.68**, GitHub team-scoped model policy. It is a preview, rolls out broadly on August 3, and uses least-restrictive union semantics.

## Evidence discipline

- External repositories were inspected read-only.
- No external code was cloned, installed, built, imported, or executed.
- The arXiv scan found no Saturday listing batch, and Friday carry-forwards were deduplicated against the July 31 synthesis.
- NotebookLM remained disabled. No podcast command, audio artifact, or `.notebooklm-sync.json` edit was made.
